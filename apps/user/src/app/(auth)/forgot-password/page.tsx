import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Forgot Password | Baladi',
};

export default function ForgotPasswordPage() {
  return <div>ForgotPasswordPage</div>;
}
